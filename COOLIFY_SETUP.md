# Установка Coolify и настройка автоматического деплоя

## 📋 Обзор

Это руководство поможет установить Coolify на ваш сервер и настроить автоматический деплой из GitHub репозитория в папку `/root/analiz_aptechki`.

**Сервер:** `2.56.240.119`  
**Путь деплоя:** `/root/analiz_aptechki`  
**GitHub репозиторий:** `https://github.com/imuzolev/medkit`

---

## 🚀 Шаг 1: Установка Coolify на сервере

### Подключение к серверу

```bash
ssh root@2.56.240.119
```

### Установка Docker (если не установлен)

```bash
# Проверка наличия Docker
docker --version

# Если Docker не установлен, установите его:
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Запустите Docker
systemctl start docker
systemctl enable docker
```

### Установка Coolify

Coolify можно установить через официальный скрипт:

```bash
# Скачайте и запустите установочный скрипт
curl -fsSL https://cdn.coollabs.io/coolify/install.sh | bash
```

Или используйте Docker Compose напрямую:

```bash
# Создайте директорию для Coolify
mkdir -p /data/coolify
cd /data/coolify

# Скачайте docker-compose.yml
curl -o docker-compose.yml https://cdn.coollabs.io/coolify/docker-compose.yml

# Запустите Coolify
docker compose up -d
```

### Доступ к Coolify

После установки Coolify будет доступен по адресу:
- **HTTP:** `http://2.56.240.119:8000` (или другой порт, указанный при установке)
- Или через домен, если настроен

**Важно:** При первом запуске создайте административный аккаунт.

---

## 🔧 Шаг 2: Настройка проекта в Coolify

### 2.1. Создание нового проекта

1. Войдите в панель Coolify
2. Перейдите в раздел "Projects" или "Проекты"
3. Нажмите "Create New Project" или "Создать новый проект"
4. Назовите проект: `medkit` или `analiz_aptechki`

### 2.2. Добавление приложения

1. В созданном проекте нажмите "Add New Resource" или "Добавить ресурс"
2. Выберите тип: **"Application"** или **"Приложение"**
3. Выберите источник: **"GitHub"**

### 2.3. Настройка подключения к GitHub

1. Нажмите "Connect GitHub" или "Подключить GitHub"
2. Авторизуйтесь через GitHub OAuth
3. Выберите репозиторий: `imuzolev/medkit`
4. Выберите ветку: `main`

### 2.4. Настройка деплоя

В настройках приложения укажите:

- **Build Pack:** `Dockerfile` (Coolify автоматически обнаружит Dockerfile)
- **Port:** `5000`
- **Deploy Path:** `/root/analiz_aptechki` (или оставьте по умолчанию, если Coolify использует свою структуру)

### 2.5. Переменные окружения (если нужны)

Если требуются переменные окружения, добавьте их в разделе "Environment Variables":
- `FLASK_ENV=production`
- `PORT=5000`

---

## 🔄 Шаг 3: Настройка автоматического деплоя

### Вариант 1: Webhook (рекомендуется)

Coolify автоматически создаст webhook в GitHub при подключении репозитория. При каждом push в ветку `main` будет происходить автоматический деплой.

**Проверка:**
1. В настройках приложения в Coolify найдите раздел "Webhooks"
2. Убедитесь, что webhook активен
3. При необходимости скопируйте URL webhook и добавьте его в GitHub вручную:
   - Перейдите: `https://github.com/imuzolev/medkit/settings/hooks`
   - Нажмите "Add webhook"
   - Вставьте URL от Coolify
   - Content type: `application/json`
   - Events: выберите "Just the push event"

### Вариант 2: GitHub Actions (альтернатива)

Если webhook не работает, можно использовать GitHub Actions. Создайте файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to Coolify

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Coolify deployment
        run: |
          curl -X POST "${{ secrets.COOLIFY_WEBHOOK_URL }}" \
            -H "Content-Type: application/json" \
            -d '{"ref":"refs/heads/main"}'
```

---

## 📁 Шаг 4: Настройка пути деплоя

Если Coolify по умолчанию деплоит в другую директорию, можно настроить кастомный путь:

### Способ 1: Через настройки Coolify

В настройках приложения найдите опцию "Deploy Path" или "Working Directory" и укажите:
```
/root/analiz_aptechki
```

### Способ 2: Через Dockerfile

Dockerfile уже настроен на работу в `/app`. Если нужно изменить путь, отредактируйте `WORKDIR` в Dockerfile.

### Способ 3: Post-deploy скрипт

В Coolify можно добавить post-deploy скрипт, который скопирует файлы в нужную директорию:

```bash
#!/bin/bash
# Копирование файлов в /root/analiz_aptechki
cp -r /app/* /root/analiz_aptechki/
```

---

## 🧪 Шаг 5: Первый деплой

1. В панели Coolify нажмите "Deploy" или "Деплой" для вашего приложения
2. Coolify автоматически:
   - Клонирует репозиторий
   - Соберет Docker образ
   - Запустит контейнер
3. Проверьте логи деплоя в разделе "Logs"

---

## 🔍 Шаг 6: Проверка работы

### Проверка контейнера

```bash
ssh root@2.56.240.119
docker ps
```

Должен быть виден контейнер с вашим приложением.

### Проверка логов

```bash
# В Coolify UI: раздел "Logs"
# Или через Docker:
docker logs <container_id>
```

### Проверка доступности

Откройте в браузере:
- `http://2.56.240.119:5000` (если порт открыт)
- Или через домен, настроенный в Coolify

---

## 🔄 Обновление приложения

После каждого push в ветку `main` на GitHub, Coolify автоматически:
1. Обнаружит изменения
2. Пересоберет образ
3. Перезапустит контейнер

Или можно запустить деплой вручную из панели Coolify.

---

## 🛠️ Устранение проблем

### Проблема: Coolify не видит изменения

**Решение:**
1. Проверьте, что webhook активен в GitHub
2. Проверьте логи webhook в GitHub: `Settings → Webhooks → Recent Deliveries`
3. В Coolify нажмите "Redeploy" вручную

### Проблема: Ошибка сборки Docker

**Решение:**
1. Проверьте Dockerfile на ошибки
2. Проверьте логи сборки в Coolify
3. Убедитесь, что все зависимости указаны в `requirements.txt`

### Проблема: Приложение не запускается

**Решение:**
1. Проверьте логи контейнера: `docker logs <container_id>`
2. Убедитесь, что порт 5000 открыт
3. Проверьте переменные окружения в Coolify

### Проблема: Файлы не в нужной директории

**Решение:**
1. Используйте post-deploy скрипт для копирования файлов
2. Или настройте volume mapping в Coolify
3. Или используйте симлинк: `ln -s /coolify/path /root/analiz_aptechki`

---

## 📚 Полезные команды

### Просмотр контейнеров

```bash
docker ps -a
```

### Просмотр логов

```bash
docker logs -f <container_name>
```

### Перезапуск контейнера

```bash
docker restart <container_name>
```

### Остановка контейнера

```bash
docker stop <container_name>
```

### Удаление контейнера

```bash
docker rm <container_name>
```

---

## 🔐 Безопасность

1. **Firewall:** Убедитесь, что порты Coolify (обычно 8000) и приложения (5000) открыты только для нужных IP
2. **SSH ключи:** Используйте SSH ключи вместо паролей
3. **HTTPS:** Настройте SSL сертификат для Coolify и приложения
4. **Обновления:** Регулярно обновляйте Docker и Coolify

---

## 📝 Дополнительные настройки

### Настройка домена

В Coolify можно настроить домен для приложения:
1. Перейдите в настройки приложения
2. Добавьте домен (например, `aimedkit.ru`)
3. Coolify автоматически настроит nginx и SSL (если включен)

### Настройка SSL

Coolify поддерживает автоматическую настройку SSL через Let's Encrypt:
1. В настройках приложения включите SSL
2. Укажите домен
3. Coolify автоматически получит и установит сертификат

---

## ✅ Чеклист установки

- [ ] Docker установлен и запущен
- [ ] Coolify установлен и доступен
- [ ] Создан проект в Coolify
- [ ] Подключен GitHub репозиторий
- [ ] Настроен автоматический деплой
- [ ] Приложение успешно задеплоено
- [ ] Приложение доступно по адресу сервера
- [ ] Webhook настроен и работает

---

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи в Coolify UI
2. Проверьте логи Docker: `docker logs <container_id>`
3. Проверьте документацию Coolify: https://coolify.io/docs
4. Проверьте GitHub Issues: https://github.com/coollabsio/coolify/issues

---

**Готово!** Теперь при каждом push в ветку `main` ваше приложение будет автоматически деплоиться на сервер. 🎉

# Инструкция по деплою проекта на сервер Beget

## 📍 Адрес вашего сайта

После запуска приложения ваш сайт будет доступен по адресу:

**http://2.56.240.119:5000**

После настройки домена (см. раздел "🌐 Настройка домена aimedkit.ru"):
- **http://aimedkit.ru** (или **https://aimedkit.ru** с SSL)

Если порт 5000 закрыт в настройках Beget, вам нужно:
1. Открыть порт 5000 в панели управления Beget
2. Или настроить домен и проксирование через nginx (рекомендуется)

---

## 🌐 Настройка домена aimedkit.ru

### Шаг 1: Настройка DNS записей

В панели управления вашего регистратора домена (где вы купили `aimedkit.ru`) настройте DNS записи:

1. **A-запись** для домена:
   - **Имя/Поддомен**: `@` или `aimedkit.ru`
   - **Тип**: `A`
   - **Значение/IP**: `2.56.240.119`
   - **TTL**: `3600` (или автоматически)

2. **A-запись** для www (опционально):
   - **Имя/Поддомен**: `www`
   - **Тип**: `A`
   - **Значение/IP**: `2.56.240.119`
   - **TTL**: `3600`

**Важно**: Изменения DNS могут занять от нескольких минут до 24 часов. Проверить можно командой:
```powershell
nslookup aimedkit.ru
```

### Шаг 2: Добавление домена в панели Beget

1. Войдите в панель управления Beget
2. Перейдите в раздел "Домены" или "Сайты"
3. Добавьте домен `aimedkit.ru`
4. Укажите корневую директорию (если требуется)

### Шаг 3: Настройка nginx на сервере

**Способ 1: Создание файла через PowerShell (рекомендуется)**

Выполните команды в PowerShell:

```powershell
# Создайте временный файл с конфигурацией локально
$nginxConfig = @'
server {
    listen 80;
    server_name aimedkit.ru www.aimedkit.ru;

    # Логи
    access_log /var/log/nginx/aimedkit.ru.access.log;
    error_log /var/log/nginx/aimedkit.ru.error.log;

    # Проксирование на Flask приложение
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Таймауты для больших файлов
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Статические файлы (если нужно)
    location /static {
        alias /root/analiz_aptechki/static;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
'@

# Сохраните во временный файл
$nginxConfig | Out-File -FilePath nginx_aimedkit.conf -Encoding utf8 -NoNewline

# Загрузите на сервер
scp nginx_aimedkit.conf root@2.56.240.119:/etc/nginx/conf.d/aimedkit.ru.conf

# Удалите временный файл локально
Remove-Item nginx_aimedkit.conf
```

**Способ 2: Создание файла напрямую на сервере через SSH**

```powershell
# Подключитесь к серверу
ssh root@2.56.240.119
```

Затем на сервере выполните:

```bash
cat > /etc/nginx/conf.d/aimedkit.ru.conf << 'EOF'
server {
    listen 80;
    server_name aimedkit.ru www.aimedkit.ru;

    # Логи
    access_log /var/log/nginx/aimedkit.ru.access.log;
    error_log /var/log/nginx/aimedkit.ru.error.log;

    # Проксирование на Flask приложение
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Таймауты для больших файлов
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Статические файлы (если нужно)
    location /static {
        alias /root/analiz_aptechki/static;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
EOF
```

После создания файла выйдите из SSH: `exit`

### Шаг 4: Проверка и перезапуск nginx

Выполните команды в PowerShell:

```powershell
# Проверьте конфигурацию nginx на ошибки
ssh root@2.56.240.119 "nginx -t"

# Если проверка прошла успешно, перезапустите nginx
ssh root@2.56.240.119 "systemctl restart nginx"

# Включите автозапуск nginx при загрузке сервера
ssh root@2.56.240.119 "systemctl enable nginx"

# Проверьте статус nginx
ssh root@2.56.240.119 "systemctl status nginx"
```

**Или выполните все команды одной строкой:**

```powershell
ssh root@2.56.240.119 "nginx -t && systemctl restart nginx && systemctl enable nginx && systemctl status nginx"
```

### Шаг 5: Настройка SSL сертификата (HTTPS) - опционально, но рекомендуется

Для установки бесплатного SSL сертификата Let's Encrypt:

```bash
# Установите certbot (если не установлен)
# Для CentOS/RHEL:
yum install -y certbot python3-certbot-nginx
# Для Ubuntu/Debian:
# apt-get install -y certbot python3-certbot-nginx

# Получите SSL сертификат
certbot --nginx -d aimedkit.ru -d www.aimedkit.ru

# Следуйте инструкциям на экране
# Certbot автоматически обновит конфигурацию nginx
```

После установки SSL сертификата ваш сайт будет доступен по адресу **https://aimedkit.ru**

### Шаг 6: Проверка работы домена

После настройки проверьте:

1. **Проверка DNS** (с вашего компьютера):
```powershell
nslookup aimedkit.ru
```

2. **Проверка доступности сайта**:
   - Откройте в браузере: `http://aimedkit.ru` (или `https://aimedkit.ru` если настроен SSL)
   - Сайт должен открыться без указания порта

3. **Проверка логов nginx** (на сервере):
```bash
tail -f /var/log/nginx/aimedkit.ru.access.log
```

### Шаг 7: Настройка файрвола (если требуется)

Если на сервере активен файрвол, убедитесь, что порты 80 и 443 открыты:

```bash
# Для firewalld (CentOS/RHEL):
firewall-cmd --permanent --add-service=http
firewall-cmd --permanent --add-service=https
firewall-cmd --reload

# Для ufw (Ubuntu/Debian):
# ufw allow 80/tcp
# ufw allow 443/tcp
# ufw reload
```

### Важные замечания:

1. **DNS распространение**: После настройки DNS записей может потребоваться время (от 5 минут до 24 часов) для их распространения по всему миру.

2. **Проверка DNS**: Используйте онлайн-сервисы для проверки DNS:
   - https://dnschecker.org
   - https://www.whatsmydns.net

3. **Порт 5000**: После настройки nginx порт 5000 можно закрыть в файрволе, так как весь трафик будет идти через nginx на портах 80/443.

4. **Автозапуск Flask приложения**: Убедитесь, что Flask приложение запускается автоматически при перезагрузке сервера (см. раздел "Запуск приложения").

---

## 🔐 Настройка SSH ключей (чтобы не вводить пароль каждый раз)

### Шаг 1: Генерация SSH ключа на вашем компьютере

В PowerShell выполните:

```powershell
ssh-keygen -t rsa -b 4096
```

Нажмите Enter для всех вопросов (или укажите путь, если хотите).

### Шаг 2: Копирование ключа на сервер

```powershell
ssh-copy-id root@2.56.240.119
```

Или вручную:

```powershell
type $env:USERPROFILE\.ssh\id_rsa.pub | ssh root@2.56.240.119 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

После этого введите пароль **один раз**, и больше пароль запрашиваться не будет.

---

## 🚀 Команды для деплоя проекта

Выполняйте команды по порядку в PowerShell:

### 1. Переход в директорию проекта

```powershell
cd D:\ivantuz\profi
```

### 2. Создание директории на сервере

```powershell
ssh root@2.56.240.119 "mkdir -p ~/analiz_aptechki"
```

### 3. Создание файла analiz_aptechki в корне

```powershell
ssh root@2.56.240.119 "cd ~ && touch analiz_aptechki && echo 'Проект анализа аптечки' > analiz_aptechki"
```

### 4. Загрузка app.py

```powershell
scp app.py root@2.56.240.119:~/analiz_aptechki/
```

### 5. Загрузка requirements.txt

```powershell
scp requirements.txt root@2.56.240.119:~/analiz_aptechki/
```

### 6. Загрузка папки templates

```powershell
scp -r templates root@2.56.240.119:~/analiz_aptechki/
```

### 7. Загрузка папки static

```powershell
scp -r static root@2.56.240.119:~/analiz_aptechki/
```

### 8. Создание виртуального окружения на сервере

```powershell
ssh root@2.56.240.119 "cd ~/analiz_aptechki && python3 -m venv venv"
```

### 9. Обновление pip

```powershell
ssh root@2.56.240.119 "cd ~/analiz_aptechki && source venv/bin/activate && pip install --upgrade pip"
```

### 10. Установка зависимостей

```powershell
ssh root@2.56.240.119 "cd ~/analiz_aptechki && source venv/bin/activate && pip install -r requirements.txt"
```

---

## 🖥️ Запуск приложения на сервере

### Вариант 1: Запуск через SSH команду (в фоне)

```powershell
ssh root@2.56.240.119 "cd ~/analiz_aptechki && source venv/bin/activate && nohup python app.py > app.log 2>&1 &"
```

### Вариант 2: Подключение к серверу и запуск вручную

**Подключение к серверу:**
```powershell
ssh root@2.56.240.119
```

**На сервере выполните:**
```bash
cd ~/analiz_aptechki
source venv/bin/activate
python app.py
```

**Для запуска в фоне (чтобы можно было закрыть терминал):**
```bash
cd ~/analiz_aptechki
source venv/bin/activate
nohup python app.py > app.log 2>&1 &
```

---

## 🚪 Как выйти из SSH сессии

### Если вы подключены к серверу через SSH:

**Просто введите:**
```bash
exit
```

Или нажмите:
- `Ctrl + D`

### Если приложение запущено в текущей сессии:

1. Остановите приложение: `Ctrl + C`
2. Затем выйдите: `exit`

### Если приложение запущено в фоне:

Просто выйдите из SSH:
```bash
exit
```

Приложение продолжит работать на сервере.

---

## 🔍 Проверка работы приложения

### Проверка, что приложение запущено:

```powershell
ssh root@2.56.240.119 "ps aux | grep app.py"
```

### Просмотр логов (если запущено в фоне):

```powershell
ssh root@2.56.240.119 "tail -f ~/analiz_aptechki/app.log"
```

### Проверка порта:

```powershell
ssh root@2.56.240.119 "netstat -tulpn | grep 5000"
```

---

## 🛑 Остановка приложения

### Найти процесс:

```powershell
ssh root@2.56.240.119 "ps aux | grep app.py"
```

### Остановить процесс (замените PID на номер процесса):

```powershell
ssh root@2.56.240.119 "kill <PID>"
```

Или остановить все процессы Python:

```powershell
ssh root@2.56.240.119 "pkill -f app.py"
```

---

## 📝 Обновление проекта

После изменения файлов на локальном компьютере:

### 1. Загрузка обновленного app.py

```powershell
scp app.py root@2.56.240.119:~/analiz_aptechki/
```

### 2. Загрузка обновленных templates

```powershell
scp -r templates root@2.56.240.119:~/analiz_aptechki/
```

### 3. Загрузка обновленных static файлов

```powershell
scp -r static root@2.56.240.119:~/analiz_aptechki/
```

### 4. Перезапуск приложения

Сначала остановите старое:
```powershell
ssh root@2.56.240.119 "pkill -f app.py"
```

Затем запустите новое:
```powershell
ssh root@2.56.240.119 "cd ~/analiz_aptechki && source venv/bin/activate && nohup python app.py > app.log 2>&1 &"
```

---

## ⚠️ Важные замечания

1. **Порт 5000**: Убедитесь, что порт 5000 открыт в настройках Beget. Если нет, откройте его в панели управления или настройте проксирование через nginx (рекомендуется).

2. **Домен aimedkit.ru**: Настройте домен согласно инструкции в разделе "🌐 Настройка домена aimedkit.ru" для доступа без указания порта и IP-адреса.

3. **Файрвол**: Проверьте настройки файрвола на сервере. После настройки nginx порт 5000 можно закрыть, так как весь трафик будет идти через nginx.

4. **Python версия**: Убедитесь, что на сервере установлен Python 3.7 или выше:
   ```powershell
   ssh root@2.56.240.119 "python3 --version"
   ```

---

## 📚 Полезные команды

### Просмотр содержимого директории на сервере:

```powershell
ssh root@2.56.240.119 "ls -la ~/analiz_aptechki"
```

### Просмотр логов в реальном времени:

```powershell
ssh root@2.56.240.119 "tail -f ~/analiz_aptechki/app.log"
```

### Проверка использования ресурсов:

```powershell
ssh root@2.56.240.119 "top"
```

Выйти из top: нажмите `q`

---

## 🆘 Решение проблем

### Приложение не запускается:

1. Проверьте логи: `ssh root@2.56.240.119 "cat ~/analiz_aptechki/app.log"`
2. Проверьте зависимости: `ssh root@2.56.240.119 "cd ~/analiz_aptechki && source venv/bin/activate && pip list"`
3. Проверьте Python: `ssh root@2.56.240.119 "python3 --version"`

### Порт недоступен:

1. Проверьте, что приложение запущено: `ssh root@2.56.240.119 "ps aux | grep app.py"`
2. Проверьте порт: `ssh root@2.56.240.119 "netstat -tulpn | grep 5000"`
3. Откройте порт в панели управления Beget

### Ошибки при установке зависимостей:

```powershell
ssh root@2.56.240.119 "cd ~/analiz_aptechki && source venv/bin/activate && pip install --upgrade pip && pip install -r requirements.txt"
```

# 🚀 Быстрый старт с Coolify

## Шаг 1: Установка Coolify на сервер

### Вариант A: Автоматическая установка (Windows)

На вашем локальном компьютере выполните:

```powershell
.\install_coolify.ps1
```

Скрипт автоматически:
- Загрузит установочный скрипт на сервер
- Установит Docker (если нужно)
- Установит и запустит Coolify

### Вариант B: Ручная установка

Подключитесь к серверу:

```bash
ssh root@2.56.240.119
```

Затем выполните:

```bash
# Скачайте и запустите установочный скрипт
curl -fsSL https://cdn.coollabs.io/coolify/install.sh | bash
```

Или используйте наш скрипт:

```bash
# Загрузите скрипт на сервер
# (с локального компьютера)
scp setup_coolify.sh root@2.56.240.119:/tmp/

# На сервере
ssh root@2.56.240.119
chmod +x /tmp/setup_coolify.sh
/tmp/setup_coolify.sh
```

## Шаг 2: Доступ к Coolify

После установки откройте в браузере:

**http://2.56.240.119:8000**

При первом запуске создайте административный аккаунт.

## Шаг 3: Настройка проекта в Coolify

1. **Создайте новый проект:**
   - Нажмите "Create New Project" или "Создать проект"
   - Название: `medkit` или `analiz_aptechki`

2. **Добавьте приложение:**
   - В проекте нажмите "Add New Resource" → "Application"
   - Выберите источник: **GitHub**

3. **Подключите GitHub:**
   - Нажмите "Connect GitHub"
   - Авторизуйтесь через GitHub OAuth
   - Выберите репозиторий: `imuzolev/medkit`
   - Выберите ветку: `main`

4. **Настройте деплой:**
   - Build Pack: `Dockerfile` (автоматически определится)
   - Port: `5000`
   - Deploy Path: `/root/analiz_aptechki` (если нужно)

5. **Запустите деплой:**
   - Нажмите "Deploy" или "Деплой"
   - Дождитесь завершения сборки и запуска

## Шаг 4: Проверка работы

После успешного деплоя приложение будет доступно по адресу:

- `http://2.56.240.119:5000`
- Или через домен, если настроен в Coolify

## Автоматический деплой

После настройки, при каждом push в ветку `main` на GitHub, Coolify автоматически:
- Обнаружит изменения
- Пересоберет Docker образ
- Перезапустит контейнер

## Полезные команды

### Проверка статуса на сервере

```bash
ssh root@2.56.240.119
docker ps  # Просмотр запущенных контейнеров
docker logs <container_name>  # Просмотр логов
```

### Ручной перезапуск

В Coolify UI: нажмите "Redeploy" для вашего приложения

## Дополнительная информация

- 📚 Подробная инструкция: [COOLIFY_SETUP.md](COOLIFY_SETUP.md)
- 🐳 Dockerfile уже настроен в проекте
- 🔄 GitHub Actions workflow настроен (опционально)

## Устранение проблем

Если что-то не работает:

1. Проверьте логи в Coolify UI
2. Проверьте логи Docker: `docker logs <container_name>`
3. Убедитесь, что порт 5000 открыт
4. Проверьте, что webhook настроен в GitHub

---

**Готово!** 🎉 Теперь ваш проект автоматически деплоится при каждом push в `main`.

# Автоматический деплой через Coolify + GitHub

## Проблема

Ручная сборка через `coolify-helper` не работает правильно. Coolify должен управлять сборкой автоматически через webhook.

## Решение: Настройка автоматического деплоя

### 🔗 Прямые ссылки

**GitHub Webhooks:**
- Добавить webhook: `https://github.com/imuzolev/medkit/settings/hooks/new`
- Список webhooks: `https://github.com/imuzolev/medkit/settings/hooks`

**Coolify UI:**
- Обычно доступен по: `http://2.56.240.119:8000` (или ваш IP:8000)
- Настройки приложения: `http://<ваш_сервер>:8000/projects/<project_id>/applications/<app_id>/settings`

### Способ 1: Через Coolify UI (Рекомендуется)

1. **Откройте Coolify UI:**
   ```
   http://<ваш_сервер>:8000
   ```

2. **Настройте GitHub интеграцию:**
   - В Coolify UI откройте ваш проект
   - Откройте ваше приложение (кликните на него)
   - Перейдите в раздел **"Settings"** (иконка шестеренки ⚙️ вверху справа или вкладка слева)
   - Найдите раздел **"Source"** или **"Git Integration"** или **"Repository"**
   - Убедитесь, что репозиторий подключен: `imuzolev/medkit`
   - Убедитесь, что ветка: `main`
   - Если репозиторий не подключен:
     - Нажмите **"Connect GitHub"** или **"Add Source"**
     - Авторизуйтесь через GitHub OAuth
     - Выберите репозиторий `imuzolev/medkit`
     - Выберите ветку `main`

3. **Настройте Webhook:**
   
   **В Coolify UI:**
   - Откройте ваше приложение в Coolify
   - Перейдите в раздел **"Settings"** или **"Настройки"** (иконка шестеренки)
   - Найдите раздел **"Deployments"** или **"Git Integration"** или **"Source"**
   - В этом разделе должен быть **URL webhook** или кнопка **"Show Webhook URL"**
   - Скопируйте URL webhook (формат: `http://<ваш_сервер>:8000/api/v1/webhooks/github/<webhook_id>`)
   
   **Альтернативный путь в Coolify:**
   - Если не видите webhook в настройках, попробуйте:
     - `http://<ваш_сервер>:8000/projects/<project_id>/applications/<app_id>/settings`
     - Или просто откройте приложение и найдите вкладку "Settings" / "Deployments"
   
   **В GitHub:**
   - Перейдите: `https://github.com/imuzolev/medkit/settings/hooks`
   - Или: `https://github.com/imuzolev/medkit` → **Settings** (вверху справа) → **Webhooks** (слева в меню) → **Add webhook**
   - Вставьте URL webhook от Coolify в поле "Payload URL"
   - Content type: `application/json`
   - Events: выберите **"Just the push event"**
   - Active: ✅ (галочка должна быть)
   - Нажмите **"Add webhook"**

4. **Проверка:**
   - Сделайте небольшое изменение в коде
   - Сделайте commit и push в GitHub
   - Coolify автоматически обнаружит изменения и задеплоит

### Способ 2: Использование скрипта на сервере

Если webhook не работает, используйте скрипт `setup_coolify_webhook.sh`:

```bash
# На сервере
chmod +x setup_coolify_webhook.sh
./setup_coolify_webhook.sh
```

Скрипт:
- Получит последний commit из GitHub
- Обновит SOURCE_COMMIT в .env файле Coolify
- Предоставит инструкции по настройке webhook

### Способ 3: Ручной деплой через Coolify UI

Если нужно задеплоить вручную:

1. Откройте Coolify UI
2. Перейдите в ваше приложение
3. Нажмите кнопку **"Deploy"** или **"Redeploy"**
4. Coolify автоматически:
   - Получит последний commit из GitHub
   - Соберет Docker образ
   - Запустит контейнер

## Важные изменения в коде

### app.py
Обновлен для поддержки переменной окружения `PORT` (Coolify использует её):
```python
port = int(os.environ.get('PORT', 5000))
app.run(debug=False, host='0.0.0.0', port=port, use_reloader=False)
```

### Dockerfile
Рекомендуется обновить для поддержки переменной PORT:
```dockerfile
EXPOSE 5000
# Coolify автоматически пробросит порт через переменную PORT
```

## Проверка работы

### Проверка webhook в GitHub:
1. Перейдите: `https://github.com/imuzolev/medkit/settings/hooks`
2. Найдите ваш webhook в списке
3. Кликните на webhook, чтобы открыть детали
4. Проверьте "Recent Deliveries" - должны быть успешные запросы (зеленые галочки) при push
5. Если есть ошибки (красные крестики), кликните на delivery и посмотрите детали ошибки

### Проверка логов Coolify:
1. В Coolify UI откройте раздел "Logs" вашего приложения
2. При каждом push должны появляться логи сборки и деплоя

### Проверка приложения:
```bash
# На сервере
docker ps | grep <ваше_приложение>
docker logs <container_id>
```

## 🔍 Как найти Webhook URL в Coolify

Если не можете найти webhook URL в интерфейсе, попробуйте:

### Способ 1: Через настройки приложения
1. Откройте Coolify UI: `http://2.56.240.119:8000`
2. Перейдите в ваш проект → ваше приложение
3. Вкладка **"Settings"** → раздел **"Deployments"** или **"Git Integration"**
4. Ищите поле **"Webhook URL"** или кнопку **"Show Webhook"** / **"Copy Webhook URL"**

### Способ 2: Через API (на сервере)
```bash
# На сервере, в директории приложения Coolify
cd /data/coolify/applications/uksoc0wgw08gk848s4kokg8c

# Проверьте .env файл - там может быть webhook URL
cat .env | grep -i webhook

# Или проверьте через API (если есть доступ)
curl http://localhost:8000/api/v1/applications/uksoc0wgw08gk848s4kokg8c | grep -i webhook
```

### Способ 3: Формат URL webhook
Webhook URL обычно имеет формат:
```
http://<ваш_сервер>:8000/api/v1/webhooks/github/<webhook_id>
```

Например:
```
http://2.56.240.119:8000/api/v1/webhooks/github/abc123def456
```

### Способ 4: Если webhook не создан автоматически
1. В Coolify UI откройте настройки приложения
2. Убедитесь, что GitHub репозиторий подключен
3. Сохраните настройки - Coolify должен автоматически создать webhook URL
4. Если не создался, попробуйте пересоздать подключение к GitHub

## Устранение проблем

### Проблема: Webhook не срабатывает
**Решение:**
1. Проверьте, что webhook активен в GitHub
2. Проверьте "Recent Deliveries" в GitHub - должны быть успешные запросы
3. Проверьте логи Coolify на наличие ошибок
4. Убедитесь, что URL webhook правильный

### Проблема: Coolify не видит изменения
**Решение:**
1. Убедитесь, что push был в ветку `main`
2. Проверьте настройки приложения в Coolify - правильная ли ветка указана
3. Попробуйте нажать "Redeploy" вручную в Coolify UI

### Проблема: Ошибка сборки
**Решение:**
1. Проверьте Dockerfile на ошибки
2. Проверьте requirements.txt - все зависимости должны быть указаны
3. Проверьте логи сборки в Coolify UI

## Быстрая команда для обновления SOURCE_COMMIT

Если нужно вручную обновить SOURCE_COMMIT:

```bash
# На сервере
cd /root/medkit_fix  # или ваш путь к репозиторию
git pull
LATEST_COMMIT=$(git log --oneline -1 | awk '{print $1}')
cd /data/coolify/applications/uksoc0wgw08gk848s4kokg8c
sed -i "s/SOURCE_COMMIT=.*/SOURCE_COMMIT=$LATEST_COMMIT/" .env
```

Затем запустите деплой через Coolify UI (кнопка "Deploy").

## Рекомендации

1. **Используйте webhook** - это самый надежный способ автоматического деплоя
2. **Не используйте ручную сборку через coolify-helper** - это может привести к проблемам
3. **Всегда проверяйте логи** в Coolify UI после деплоя
4. **Делайте небольшие изменения** и тестируйте после каждого деплоя

---

**Готово!** Теперь при каждом push в `main` ваше приложение будет автоматически деплоиться через Coolify. 🚀
